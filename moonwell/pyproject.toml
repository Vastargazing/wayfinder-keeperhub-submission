[project]
name = "moonwell-keeperhub-demo"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = ["keeperhub-wayfinder-executor==0.1.0"]

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
packages = ["moonwell_demo"]
