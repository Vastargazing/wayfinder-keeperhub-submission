# Snapshot decisions

This candidate preserves client behavior and existing test bodies from baseline
`5d41778ca3d84839efe0c5df810553c251c0c744`. Documentation and build/check metadata
are adapted to this source snapshot. [The command map](command-map.md) names the
coverage difference. No test is silently skipped or weakened.

An uncertain result retains its operation identity. Recovery requires ownership,
content and fresh verification, independently of balances and allowances.
The client-side refusals remain part of the product.

Access and original-material licensing await owner decisions. The snapshot is a
local candidate; no distribution, new live cycle or server deployment is implied.
